# Define a function with an output
def format_name(f_name, l_name):
    """
    Docstring example. Takes a first and last names, join and format them to title case.
    """
    return f"{f_name} {l_name}".title()


# Get the output of a function
print(format_name("jAnE", "doE"))
